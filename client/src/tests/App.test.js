describe('Testing game logic', () => {
  beforeEach(() => {})

  test('Must not deal a card that already exists', () => {
    const testFunction = () => {
      return 1
    }

    expect(testFunction()).toEqual(1)
  })
})
